// declare of variables 
let num=10;
console.log( typeof(num));// we use here type of keyword so that we could know the type of datatype
// Primitive datatypes
// number,boolean, string,null,undefined,symbol,bigint
let account_balance =20;
console.log(account_balance);
// string
let str="arpit"; 
console.log(str);
// string
let comment='hello frnds';
console.log(comment);

// Boolean 
 let My_name_is_arpit = true;
 console.log(My_name_is_arpit);

 //undefined
 let account;// there is no value present so it gives us undefined
 console.log(account);
 console.log( typeof account);
 
 // NULL
 let balance=null;
 console.log( balance);
 console.log(typeof balance);// null gives object in return (and it is the biggest mistake ever , since null is datatype, telling us , it is object)

 // Bigint

 let a= 3536375685637536574547547475n;// put n in the last of the number , so that we can recognize that it is bigint
 console.log(a);  